<div class="container margin-nav">
  <div class="row d-flex justify-content-center">
    <div class="col-md-8">
      <h1><%= text %></h1>
      <%= simple_form_for [flat, booking] do |f| %>
        <%= f.input :start_date %>
        <%= f.input :end_date %>
        <%= f.button :submit, class: 'btn btn-principal' %>
      <% end %>
    </div>
  </div>
</div>

